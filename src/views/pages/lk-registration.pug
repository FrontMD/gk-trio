extends ../layouts/default

block variables
    - const title = "Регистрация"
    - const isLK = true

block content
    .page(data-js="page")
        .page-layout
            +bc-section({
                bc: {
                items: [
                    {
                    name: 'ГК ТРИО',
                    link: 'javascript:void(0);'
                    },
                    {
                    name: 'Регистрация',
                    link: 'javascript:void(0);'
                    }
                ],
                },
            })

            +pa-auth({
                page: 'reg'
            })
