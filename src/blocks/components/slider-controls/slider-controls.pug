-
    const defaultSliderControlsProps = {
        classes: ''
    }

mixin slider-controls(props= defaultSliderControlsProps)
    -
        const checkProps = {
            classes: props.classes ? props.classes : defaultSliderControlsProps.classes,
            hideToDisable: props.hideToDisable ? props.hideToDisable : defaultSliderControlsProps.hideToDisable,
        }

    .slider-controls(class= checkProps.classes data-js="sliderControls")&attributes(attributes)
        button.slider-control.slider-control--prev(type="button" data-js='sliderPrev')
            svg
                use(xlink:href=`img/sprites/sprite.svg#arrow_slider_prev`)
        .slider-controls__separator
        button.slider-control.slider-control--next(type="button" data-js='sliderNext')
            svg
                use(xlink:href=`img/sprites/sprite.svg#arrow_slider_next`)